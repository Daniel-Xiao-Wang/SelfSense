# Frontend (`react.b`)

React UI for SelfSense: searchable symptom picker, selected-symptom chips, and ranked diagnosis results.

## Run

```bash
npm install
npm start
```

Opens [http://localhost:3000](http://localhost:3000) and calls the Flask backend at `http://127.0.0.1:5000`.
